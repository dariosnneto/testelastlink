{
  "name": "mock-payments-api-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test ",
    "test:api": "playwright test --project=api ",
    "test:ledger": "playwright test --project=ledger ",
    "test:integration": "playwright test --project=integration ",
    "test:resilience": "playwright test --project=resilience ",
    "test:concurrency": "playwright test --project=concurrency "
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0"
  }
  
}
